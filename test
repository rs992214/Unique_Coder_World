# Route for handling the login page logic..
@app.route('/login', methods=['GET', 'POST']) // method that used for login request
def login():
    error = None
    if request.method == 'POST':
        if request.form['username'] != 'admin' or request.form['password'] != 'admin':
            error = 'Invalid Credentials. Please try again //error code for wrong creds
        else: 
            return redirect(url_for('home'))
    return render_template('login.html', error=error)


#approve kar de bhai garib ki doua lyge
